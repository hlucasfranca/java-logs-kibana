PUT _ingest/pipeline/java-logs-pipeline
{
  "description": "Parse logs Java padrão",
  "processors": [
    {
      "grok": {
        "field": "message",
        "patterns": [
          "%{TIMESTAMP_ISO8601:timestamp} %{LOGLEVEL:log.level} +%{JAVACLASS:log.logger} - %{GREEDYDATA:message}"
        ]
      }
    },
    {
      "date": {
        "field": "timestamp",
        "formats": ["yyyy-MM-dd HH:mm:ss,SSS"]
      }
    }
  ]
}
